import React from 'react';
import './Placemap.css'

const PlaceMap = ({place}) => {
    
    const {map_link} = place;
    return (
        <div>
            
        </div>
    );
};

export default PlaceMap;